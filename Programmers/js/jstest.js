/*
21_07_07 create.
*/

var set = new Set();
var arr = new Array();
var maxCount;

var makeSet = function (count, idx)
{
    if(count == maxCount)
    {
        
    }
    
    for(var i=idx; i<numsLen; ++i)
    {
        
    }
}

function solution(nums) {
    var answer = 0;
    var numsLen = nums.length;
    
    maxCount = numsLen / 2;
    
    // 함수 선언에 문제있음!
    //makeSet(0, 0);

    return answer;
}